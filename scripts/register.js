// select form by Id
const registerForm = document.getElementById('register-form');  // an id is tageted and then selected ...target the id in the html//
// Handle form submit event
registerForm.onsubmit = function () {
    // collect username
    // collect email
    // collect password
    // save user information
    // send confirmation email
} //when you submit what will happen ..a function should be invoke to get the work done, knowing what you need to//